# s&box MCP

An MCP server that runs inside the s&box editor. Connect Claude Code, Cursor, Claude Desktop or VS Code and let AI work the editor: scenes, GameObjects, components, assets, ModelDoc, AnimGraph, ShaderGraph, ActionGraph, code and diagnostics.

*Full README lands with the first release — see the MCP dock in the editor for setup.*
